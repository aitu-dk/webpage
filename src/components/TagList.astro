---
import Badge from "@components/Badge";

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap">
  {
    tags.map((tag: string) => (
      <Badge>
        <strong class="text-sm font-light">#{tag}</strong>
      </Badge>
    ))
  }
</div>
